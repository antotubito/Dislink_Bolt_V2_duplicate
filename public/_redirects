# Netlify redirects file for SPA
# Handle all routes by serving index.html for client-side routing

# API routes (if any) - add specific rules here if needed
# /api/* /api/:splat 200

# Supabase redirects for authentication
/auth/callback/* /auth/callback/index.html 200

# Handle all other routes with index.html for client-side routing
/* /index.html 200